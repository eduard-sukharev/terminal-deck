"""YAML configuration files for the cyberdeck build.

Config is loaded by ``utilities.config_loader`` into validated dataclasses.
Every dimension in the build must originate from these files, a measured
component, or calculated geometry.
"""
